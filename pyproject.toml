[project]
name = "pyflink-stream-processing-with-kafka-redpanda-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sharabantahura110", email = "55558006+Sharabantahura110@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "kafka-python>=3.0.11",
    "pandas>=3.0.5",
    "psycopg2-binary>=2.9.12",
    "pyarrow>=25.0.1",
]

[project.scripts]
pyflink-stream-processing-with-kafka-redpanda-python = "pyflink_stream_processing_with_kafka_redpanda_python:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "dependency>=0.0.3",
    "jupyter>=1.1.1",
]
